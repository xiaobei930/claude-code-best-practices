{
  "description": "Claude Code Best Practices plugin hooks - DISABLED by default due to ${CLAUDE_PLUGIN_ROOT} expansion issue on Windows. Run /setup --hooks to enable hooks with absolute paths.",
  "hooks": {}
}
